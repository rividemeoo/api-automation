pipeline {
    agent any
    
    environment {
        envVarValue = "env.PYTHON_ENVIRONMENT"
    }
    
    stages {
        stage('Checkout') {
            steps {
                // Checkout the repository
                checkout([$class: 'GitSCM', branches: [[name: '*/master']], 
                          userRemoteConfigs: [[url: 'https://github.com/rividemeoo/api-automation.git']]])
            }
        }

        stage('Run PowerShell Script') {
            steps { 
                    powershell '''
                    $inputFile = "Python\\tests\\config.py"
                    $replacementValue = "engr.properties"

                    (Get-Content $inputFile) | ForEach-Object {
                        if ($envVarValue -ne $null){
                        $_ -replace "test\\.properties", $envVarValue.properties
                        }
                        else{
                        $_ -replace "test\\.properties", $replacementValue
                        }
                    } | Set-Content $inputFile

                    echo "Config replacement complete."
                    '''
            }
        }

        stage('Build') {
            steps {
                bat 'python -m pip install -r requirements.txt'
            }
        }
        
        stage('Test') {
            steps {
                bat 'python -m pytest'
            }
        }
        
        // Add more stages as needed
        
    }

    post {
        always {
            def status = currentBuild.currentResultIsBetterOrEqualTo(hudson.model.Result.SUCCESS) ? "Succeeded" : "Failed"
            emailext subject: "Build ${status}: ${currentBuild.fullDisplayName}",
                    body: """
                    Build ${status}: ${currentBuild.fullDisplayName}
                    Check the console output for details.
                    """,
                    to: 'test_jenkins_email@loopup.co, rividemeo@erraipasifik.com',
                    replyTo: 'noreply@loopup.co'
            }
    }
}